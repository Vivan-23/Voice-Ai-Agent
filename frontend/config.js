/**
 * Coway AI Platform - Public Frontend Runtime Configuration
 * 
 * If you deploy the backend on Render, set your Render public URL below:
 * e.g. "https://coway-ai-backend.onrender.com"
 * 
 * Note: You can also override this at runtime in the browser using:
 * 1. The "?api=https://your-backend.onrender.com" query parameter
 * 2. The in-app Backend Connection Settings modal
 */
window.COWAY_CONFIG = {
  // Replace with your actual Render service URL once deployed
  BACKEND_URL: "https://voice-ai-agent-vfg5.onrender.com",
};
